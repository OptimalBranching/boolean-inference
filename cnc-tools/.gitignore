# Generated by `make`: built binaries, the Proofix clone, and scratch build
# trees. Only the Makefile + README (the reproducible recipe) are tracked —
# run `make` to reconstruct everything below from upstream sources.
/bin/
/proofix/
/.build/
